const path = require('path');
const base = "./src/database/";

module.exports = {
  "config": path.resolve(base + 'config', 'config.json'),
  "models-path": path.resolve(base + '/models'),
  "migrations-path": path.resolve(base + '/migrations')
};